<h1>投稿一覧</h1>

<div class="row">
  <div class="col-md-12">
    <% @posts.each do |p| %>
      <%= render partial: 'home/post', locals: { post: p } %>
    <% end %>
  </div>
</div>
<%= paginate @posts%>